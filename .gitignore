.venv
.venv-*
_output
.up
result
.direnv
__pycache__/

# Docs preview #
######################
# The docs-site checkout and the site the preview build writes. See
# vercel.json.
/.docs-site/
/public/

# Local superpowers planning artifacts (specs/plans/notes) — not repo content
docs/superpowers/

# Local demo manifests at the repo root (may hold real credentials) — only the
# top-level directory, not docs/manifests/recipes or docs/content/recipes.
/examples/
